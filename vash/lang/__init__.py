"""Per-language security-hints KB.

Ported verbatim from VVAH (`~/visa-harness/vvaharness/lang/hints.py`) — see
`audit.lang.hints` for the extension→language map, per-language researcher
hints, and specialist hint bodies.
"""
