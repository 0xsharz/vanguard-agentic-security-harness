{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Report",
  "description": "Final structured report — only reachable, deduped findings.",
  "type": "object",
  "required": ["run_id", "target", "summary", "findings"],
  "additionalProperties": false,
  "properties": {
    "run_id": { "type": "string" },
    "target": {
      "type": "object",
      "required": ["repo_path"],
      "additionalProperties": false,
      "properties": {
        "repo_path": { "type": "string" },
        "commit": { "type": "string" }
      }
    },
    "summary": {
      "type": "object",
      "required": ["total", "by_severity"],
      "additionalProperties": false,
      "properties": {
        "total": { "type": "integer", "minimum": 0 },
        "by_severity": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "critical":      { "type": "integer", "minimum": 0 },
            "high":          { "type": "integer", "minimum": 0 },
            "medium":        { "type": "integer", "minimum": 0 },
            "low":           { "type": "integer", "minimum": 0 },
            "informational": { "type": "integer", "minimum": 0 }
          }
        }
      }
    },
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["finding_id", "title", "severity", "vuln_class", "file", "line_start", "line_end", "description", "evidence", "trace", "recommendation"],
        "additionalProperties": false,
        "properties": {
          "finding_id": { "type": "string" },
          "title": { "type": "string", "minLength": 5 },
          "severity": { "type": "string", "enum": ["critical", "high", "medium", "low", "informational"] },
          "vuln_class": { "type": "string" },
          "cwe": { "type": "string", "pattern": "^CWE-[0-9]+$" },
          "file": { "type": "string" },
          "line_start": { "type": "integer", "minimum": 1 },
          "line_end": { "type": "integer", "minimum": 1 },
          "description": { "type": "string", "minLength": 30 },
          "evidence": { "type": "string" },
          "trace": {
            "type": "object",
            "required": ["entry_points", "call_chain"],
            "additionalProperties": false,
            "properties": {
              "entry_points": {
                "type": "array",
                "items": {
                  "type": "object",
                  "required": ["kind", "location"],
                  "additionalProperties": false,
                  "properties": {
                    "kind": { "type": "string" },
                    "location": { "type": "string" },
                    "controllable_by": { "type": "string" }
                  }
                }
              },
              "call_chain": {
                "type": "array",
                "items": {
                  "type": "object",
                  "required": ["file", "function", "line"],
                  "additionalProperties": false,
                  "properties": {
                    "file": { "type": "string" },
                    "function": { "type": "string" },
                    "line": { "type": "integer", "minimum": 1 }
                  }
                }
              }
            }
          },
          "poc": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "language": { "type": "string" },
              "code": { "type": "string" },
              "succeeded": { "type": "boolean" }
            }
          },
          "variants": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Other finding_ids in the same dedupe group."
          },
          "needs_poc": {
            "type": "boolean",
            "description": "Carried from trace.exploitability.needs_poc, when present. True means static reasoning alone could not settle exploitability — the report must present this finding as confirmed_static / needs PoC, never as certain."
          },
          "exploitability_note": {
            "type": "string",
            "description": "Carried from trace.exploitability (narrative + attack_input), when present. Advisory context only — does not affect `severity`, which is CVSS-derived (V4) and authoritative."
          },
          "recommendation": { "type": "string", "minLength": 20 }
        }
      }
    },
    "input_inventory": {
      "type": "array",
      "description": "Resolved completeness ledger — every attacker-controllable input Recon enumerated, each with its reconciled disposition (covered|uncovered). Injected from the run state by report.py; a completeness artifact, not a per-finding record.",
      "items": {
        "type": "object",
        "required": ["id", "source_type", "location", "trust_level", "disposition"],
        "additionalProperties": false,
        "properties": {
          "id": { "type": "string" },
          "source_type": { "type": ["string", "null"] },
          "location": { "type": ["string", "null"] },
          "variable": { "type": ["string", "null"] },
          "entry_point": { "type": ["string", "null"] },
          "trust_level": { "type": ["string", "null"] },
          "disposition": { "type": ["string", "null"], "description": "covered | uncovered | null (unreconciled)" },
          "disposition_evidence": { "type": ["string", "null"] }
        }
      }
    }
  }
}
